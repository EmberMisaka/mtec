spring.application.name=mtec

# Ambiente ativo por padrao (dev). Em producao isso e sobrescrito
# por variavel de ambiente: SPRING_PROFILES_ACTIVE=prod
spring.profiles.active=dev

# --- Upload de imagem dos itens ---
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB